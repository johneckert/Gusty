import initialState from './initialState';
import {} from '../actions/actionTypes';

export default function cities(state = initialState.cities, action) {
  switch (action.type) {
    default:
      return state;
  }
}
